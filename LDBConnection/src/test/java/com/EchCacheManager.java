package com;

/*
import org.ehcache.Cache;
import org.ehcache.CacheManager;
import org.ehcache.config.builders.CacheConfigurationBuilder;
import org.ehcache.config.builders.CacheManagerBuilder;
import org.ehcache.config.builders.ResourcePoolsBuilder;
*/

/**
 * Created by leroy:656515489@qq.com
 * 2018/4/28.
 */
public class EchCacheManager {
  /*  private static final CacheManager cacheManager = CacheManagerBuilder.newCacheManagerBuilder().build(true);

    public static  <K,V> Cache<K, V> getCache(String cacheName, Class<K> cl, Class<V> cls){
        return cacheManager.createCache(cacheName, CacheConfigurationBuilder.newCacheConfigurationBuilder(
                        cl,  cls,ResourcePoolsBuilder.heap(10000)).build());
    }*/
}
